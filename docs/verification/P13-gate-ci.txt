# P13 gate — recorded by tools/p13-gate-check.py
# command: python3 tools/p13-gate-check.py --skip-heavy
# date: 2026-09-21

PASS every one of the matrix's 43 rows resolves to a collected test
PASS matrix canary: a row naming a test that does not exist is refused
PASS matrix canary: a run where pytest collected nothing is refused
FAIL matrix canary: a run that skipped two thirds of the mapped tests is refused  — the checker accepted a run that did not execute every mapped test
FAIL matrix canary: a passing test named in the warnings summary is not read as a failure  — the checker accepted a green run turned red by a warning line
PASS the P13 test files import and collect (96 test ids)
PASS the recorded contract fixtures still match the product
PASS contract canary: a perturbed fixture (normalised) is refused by the checker
PASS all 9 recorded chaos drills pass and their artifacts exist
PASS chaos canary: a recorded FAIL is reported, not counted
PASS chaos canary: a drill with no written expectation is refused
FAIL the load harness's quick run passes and the kit's 30-minute soak meets its clause  — the run was 0 s, not the kit's 1800 s; the run covered 0 s of wall clock for a 1800 s paced soak — it did not drive the rate it claims; the run drove None fills/s, not 200; only None of 360000 fills were consumed; no alerts fired, so the alert path was not under load at all
PASS load canary: a soak with duplicates, or a short one, is refused
PASS the frontend P13 suites exist and cover the kit's flows (3 specs)
PASS frontend canary: a missing suite or a thinned browser suite is refused
PASS the browser suite keeps the Telegram webview profile and the layout claim
PASS the project's own test command collects these files (no suite that only this gate runs)
PASS the frontend P13 suites are deferred to the `web` job (there is no node_modules here)
PASS the CI gates cover PR, nightly and release, run this phase's checks, and never gate coverage
PASS ci canary: a workflow that runs nothing is refused
PASS every lint rule fires on a planted violation
PASS the OpenAPI contract audit passes
PASS the SQLite migration subset is current
FAIL the P12 gate still passes  — SS the API entry names its own persistence caveat                        
PASS canary: the Mini App's middleware does not pass every path through    
PASS canary: a noindex header in web/vercel.json is absent                 

p12-gate-check: 34 passed, 1 failed  (--live skipped: no network probes)


p13-gate-check: 20 passed, 4 failed  (--skip-heavy)
