P04 gate: 55/55 checks passed
  PASS suite: 156 tests, exit 0, tail 'OK'
  PASS lint-rules clean (lint-rules: 34 files scanned, 0 findings)
  PASS every lint rule fires on a planted violation (8/8 rules fire)
  PASS no float/NUMERIC money column in Postgres migrations (0 found)
  PASS NUMERIC only for the three metadata columns (['minimum_order_size', 'minimum_tick_size', 'risk_latency_ms'])
  PASS the three NUMERIC metadata columns are actually declared (3/3)
  PASS the float boundary is exactly ONE assignment, inside to_float_for_sdk (1)
  PASS contract: Price is a string pattern, not a number
  PASS kill switch is the first check, and the order is ascending
  PASS Decision.notional_micro defaults to 0 and deny() never sets it (a refusal must not spend the 24h cap)
  PASS limits come from flags at request time, not a module constant
  PASS begin() answers 'did I create this row?' with RETURNING (sqlite) and xmax=0 (postgres)
  PASS Record exposes replay/busy/mismatch as properties (state alone cannot tell the owner from a waiter)
  PASS every denial after begin() releases the key (5 returns audited, 0 unpaired)
  PASS body shape is checked BEFORE idem.begin (a 422 must not burn a key)
  PASS a rejected intent is REVISED, not re-inserted (one key, one row; a bare INSERT 500s the retry against UNIQUE(user_id, idempotency_key))
  PASS openapi audit: 82 checks passed, 0 failed
  PASS openapi audit self-test fires: 13/13
  PASS generated SQLite subset is current
  PASS seed.sql stamps with {{NOW_MS}}, never an absolute timestamp (0 literals found)
  PASS run-sql records a ledger and refuses drift on an applied file
  PASS dollar-quote-aware statement splitter exists (naive split on ';' breaks triggers)
  PASS cash_ledger is append-only by trigger
  PASS flag_audit is append-only by trigger
  PASS kill_switch_state is append-only by trigger
  PASS audit_log is append-only by trigger
  PASS kill_switch reason length is a DB CHECK, not a code comment
  PASS cash_ledger's uniqueness is (ref_table, ref_id, kind, user_id)
  PASS no index leads with book_levels.updated_ms (25 such index statements checked)
  PASS there is no payout_micro column (a payout is a ledger kind, never a mutable field)
  PASS readiness REFRESHES the store, so a pod recovers without a restart
  PASS boot read happens at import, so a fresh pod is not stuck 'not ready'
  PASS numeric flags never carry `on` (bool(1234) would enable a feature)
  PASS a flag write needs a reason >= 4 chars and lands in flag_audit
  PASS Flags has no boot_defaults_used field (it lives on the store, where it is observable)
  PASS decision sections D1-D8 present (D1,D2,D3,D4,D5,D6,D7,D8,D9)
  PASS the compose path is marked [UNVERIFIED], not silently assumed
  PASS D2 states the measured SDK fact that decided the stack (V2 args are float)
  PASS D4 carries the four measured precision numbers
  PASS the environment's missing tools are listed (docker/psql/sqlite3 CLI)
  PASS every check in this gate is claimed with a number, not an adjective
  PASS no credential-shaped string in 261 working-tree files (2 patterns, allowlist enforced by exact line)
  PASS every secret-scan exemption still matches a line (a dead exemption would hide the next real key)
  PASS the scan covers the working tree, not just what git has tracked (a phase in progress is exactly when a key gets pasted)
  PASS .env is ignored by git
  PASS the env example has no values an attacker can use
  PASS every compose build target exists (7 services, 0 missing)
  PASS compose strict-parses on the first pass (147 lines, 0 duplicate keys)
  PASS compose parses and names the P04 surface
  PASS .env is optional-at-boot for services (required: false), so a fresh clone starts
  PASS no make recipe pipes a check into tail (a pipe hides the exit status) (0 found)
  PASS every promised target exists (all 13)
  PASS every tool a make target runs is on disk (13 referenced, 0 missing)
  PASS make doctor runs and reports the tooling honestly
  PASS envelope demo over real sockets (12 steps)
gate is a floor: any FAIL here means the phase is not done, whatever the prose elsewhere says
