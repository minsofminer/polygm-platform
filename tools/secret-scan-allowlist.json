{
  "note": "P07 D6 / P14 D3. Every entry is a fact about a shape that is *not* a secret, with the reason written here rather than as a grep exclusion nobody can see. `tools/ci-log-scan.py --sources` and `tools/p14-appsec-scan.py` both read this file; the scan prints how many lines it skipped.",
  "paths": [
    {
      "path": "tests/fixtures/**",
      "why": "captured venue fixtures: condition ids, token ids and tx hashes are public identifiers and have the 64-hex shape"
    },
    {
      "path": "docs/**",
      "why": "phase documents and recorded evidence quote command output, which contains ids, hashes and redacted material"
    },
    {
      "path": "web/public/**",
      "why": "static assets; nothing secret is inlined at build time by design (P08)"
    }
  ],
  "strings": [
    {
      "string": "x-access-token:$TOKEN@github.com",
      "why": "tools/repo-recover.sh builds the remote URL from a shell variable read out of ~/.secrets/tokens.env; the interpolation is the safe form, and this is the line someone writes during an incident"
    },
    {
      "string": "postgresql://polygm:polygm@pgbouncer",
      "why": "the dev compose DSN, with the dev-only password written down next to it in the same file"
    }
  ],
  "allow": []
}
